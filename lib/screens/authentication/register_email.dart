import 'package:flutter/material.dart';

class RegisterEmail extends StatelessWidget {
  const RegisterEmail({Key? key, required this.email}) : super(key: key);
  final String email;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
